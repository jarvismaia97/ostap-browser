{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Ostap Browser",
  "windows": ["main", "browse-*"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
